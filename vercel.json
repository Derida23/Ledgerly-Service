{
  "version": 2,
  "installCommand": "pnpm install && npx prisma generate && npx prisma migrate deploy",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
